<h2>メニュー編集</h2>
<%= form_with model: @menu, url:admin_menu_path(@menu), local:true do |f| %>
  <h4>メニュー名</h4>
  <%= f.text_field :name %>
  <h4>メニュー説明</h4>
  <%= f.text_area :caption %>
  <h4>金額</h4>
  <%= f.text_field :amount %>円（税込）
  <%= f.submit '変更を保存' %>
<% end %>
